using System;

namespace VulkanSample
{
    class Program
    {
        static void Main(string[] args)
        {
			//LoaderData data = TinyObjLoader.LoadObj("stanford-dragon.obj", null, true);



			//List<Vertex> vertices = new List<Vertex>();
			//List<uint> indices = new List<uint>();


			//foreach (Shape shape in data.Shapes)
			//{
			//	foreach (IndexT index in shape.Mesh.Indices)
			//	{
			//		Vertex vertex = new Vertex
			//		{
			//			pos = new Vector3()
			//			{
			//				X = data.Attrib.Vertices[3 * index.VertexIndex + 0],
			//				Y = data.Attrib.Vertices[3 * index.VertexIndex + 1],
			//				Z = data.Attrib.Vertices[3 * index.VertexIndex + 2]
			//			},


			//			textCoord = new Vector2()
			//			{
			//				X = data.Attrib.Texcoords[2 * index.TexcoordIndex + 0],
			//				Y = data.Attrib.Texcoords[2 * index.TexcoordIndex + 1] * -1
			//			}
			//		};



			//		vertices.Add(vertex);
			//		indices.Add((uint)(vertices.Count - 1));
			//	}
			//}




			//foreach (Vertex v in vertices)
			//{
			//	Console.WriteLine(v.pos.ToString());
			//}



			//foreach (var i in indices)
			//{
			//	Console.WriteLine(i);
			//}
		}
	}
}
